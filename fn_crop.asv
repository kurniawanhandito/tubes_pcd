function imgCrop = fn_crop(img,sx,sy,fx,fy)
%fn_crop Summary of this function goes here
%   Detailed explanation goes here
[row,col,page]=size(img);
for p=1:page
    for i=1:fx-sx+1
        sx=i;
        for j=1:fy-sy+1
            imgCrop(i,j,p)=img(sx,sy,p);
            sy=s
        end
    end
end